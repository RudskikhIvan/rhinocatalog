<% if @product.errors.any? %>	
	$("#errors").html('<%= escape_javascript(render("rhinocatalog/shared/error_messages", object: @product, :formats => [:html])) %>');
<% else %>
	location.href = '<%= @redirect_to %>';
<% end %>
